<?php

if (! function_exists('format_bank_account')) {
	function format_bank_account($account, $formated = true)
	{
		/*$account_formated = '';
		for ($i = 0; $i < $account; $i++) {
			if ($formated && in_array([4, 8, 10], $i) .includes(i) && account.charAt(i) !== "-") {
                	account_formated += '-';
                }
            	account_formated += account.charAt(i);
            }

            return account_formated;*/
	}
}